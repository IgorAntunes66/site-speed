import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    // Nós precisamos preencher isso!
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
export default config;
